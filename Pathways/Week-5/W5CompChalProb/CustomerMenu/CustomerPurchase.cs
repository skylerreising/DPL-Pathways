using System;
using System.Linq;

namespace Members
{
    class CustomerPurchase
    {
        // public string CustomerPurchase(int accountID, decimal amountOfPurchase)
        // {
        //     // loop to see if the account exists and if it does, call Purchase()
        //     if(allMembers.Any(x => x.AccountID = accountID))
        //     {
                    // if(amountOfPurchase > 0)
                    // {
                    //call the function
                    // }
        //     }
        // }
    }
}