using System;
using System.Linq;

namespace Members
{
    class CustomerReturnTransaction
    {
            // //loop to see if the account exists and if it does, call ReturnTransaction
            // for(int i=0; i<myMembers.Count; i++)
            // {
            //     if(myMembers[i].AccountID == accountID)
            //     {
            //         if(amountOfReturn > 0)
            //         {
            //             call the function
            //         }
            //     }
            // }
    }
}